# Build outputs — multi-gigabyte, far over GitHub's file-size limit
*.iso
*.iso.sha256
out/

# Generated at build time (the script writes these on first run)
recovery-exclude.list

# Build work directories
recovery-build/
archiso-work/
